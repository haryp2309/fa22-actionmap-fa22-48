.row.mt-2
    .col-12.col-md-6.offset-md-3.col-xl-4.offset-xl-4
        Edit News Item
        %br
        Reresentative = #{@news_item.representative.name}
        %br
        Issue = #{@news_item.issue.name}
        %br
        %br
        Select an Article and Rate:
        %br/
        %br/
        = form_for @news_item,  url: create_my_news_item_path, method: :post  do |f|
            .form-group.col
                = f.hidden_field :representative_id, value: @news_item.representative_id
                = f.hidden_field :issue_id, value: @news_item.issue_id
                = f.collection_radio_buttons :json, @top, :json, :title  do |b|
                    .row
                        .col-xs
                            = b.radio_button
                        .col
                            %b
                                = b.label
                            %p
                                = b.object.description
            .form-group.row
                .col-sm-2
                    = label :rating, :stars_count, class: 'col-form-label'
                .col-sm-10
                    = select :rating, :stars_count, @available_ratings,
                    class: 'form-control'
            .actions
                = f.submit 'Save', class: 'btn btn-primary'
